package exc;

import exc.CheckedException;

public class App {
	/*
	 * Try catch throw
	 * 
	 * throw=if a methode can raise an exception 
	 * finally=trying to acess resources 
	 * try{
	 * }catch{}
	 * finally
	 * 
	 *  checked=when specified in the throw clauses of the method
	 *  - exception, ioexception, filenotfound, parse exception, 
	 *  unchecked=exceptions are raised by system when runtime logic is deviated
	 * 

	try {// any piece of code which might raise an exception is put in try block
		code
		
	}catch(exception e) {//contains exception handler
		handler code
	}
	*/
	/*
	public static void main(String[] args) throws CheckedException
	
	{
		
		App  ex= new App();
		ex.deposit(400);
		
		try {
			Service.withdrawal(2000);
		}catch(CheckedException e) {
			System.out.println("your avilable balance is too low");
		}
		
	}
	*/
}
